import CustomizedPackagePage from "../pages/CustomizedPackagePage";

export const metadata = {
  title: "Customized Package | Sabary Tours",
  description:
    "Request a tailored Ghana tour package — share your group size, interests, dates, and budget.",
};

export default function CustomizedPackageRoute() {
  return <CustomizedPackagePage />;
}
